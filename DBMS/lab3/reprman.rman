connect target;
CONFIGURE CHANNEL DEVICE TYPE DISK FORMAT '/u01/uhy57/backup/rman/rman_%d_%U';
CONFIGURE RETENTION POLICY TO RECOVERY WINDOW OF 2 DAYS;
BACKUP AS BACKUPSET DATABASE PLUS ARCHIVELOG;
exit;