#include <stdio.h>
#include <stdlib.h>
#include "parser.h"

int main(int argc, char *argv[]) {

    system("ls");

    parsedArgs parsedArgs;

    int *a = NULL;

    parsedArgs.flags = a;


    return 0;
}